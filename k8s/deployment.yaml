apiVersion: apps/v1
kind: Deployment
metadata:
  name: primeapp-deployment
  namespace: default
  labels:
    app: primeapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: primeapp
  template:
    metadata:
      labels:
        app: primeapp
    spec:
      containers:
      - name: primeapp
        image: gaurav0934/primeapp:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: primeapp-service
  namespace: default
  labels:
    app: primeapp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: primeapp
